# 规则：生成产品需求文档（PRD）

## 目标（Goal）

指导 AI 助手基于用户的初始需求提示，产出一份结构化、可执行、适合初级开发者理解与落地实施的 PRD（Markdown 格式）。

## 流程（Process）

1.  接收初始提示：用户提供新功能或能力的简要描述/请求。
2.  提问澄清问题：在撰写 PRD 之前，AI 必须提出澄清问题以收集足够细节。目标是搞清楚“做什么”和“为什么”，而不一定是“怎么做”（由开发者决定）。请尽量用字母/数字选项列出问题，便于我用选择作答。
3.  生成 PRD：结合初始提示与用户对澄清问题的回答，按照下方结构生成 PRD。
4.  保存 PRD：以 `prd-[feature-name].md` 命名，保存到 `/tasks` 目录。

## 澄清问题（示例）

AI 需根据具体场景自适应提问，通用方向包括：

- 问题/目标：该功能为用户解决什么问题？我们想达成的主要目标是什么？
- 目标用户：该功能的主要使用者是谁？
- 核心功能：用户应能完成哪些关键操作？
- 用户故事：请给出若干用户故事（例如：作为一名[某类用户]，我想要[执行某动作]，以便[获得某收益]）。
- 验收标准：我们如何判定功能成功交付？关键成功标准有哪些？
- 范围/边界：有哪些明确“不做”的范围（非目标）？
- 数据需求：该功能需要展示或处理何种数据？
- 设计/UI：是否有现成设计稿或 UI 规范可遵循？若无，请描述期望外观与交互风格。
- 边界情况：有哪些潜在边界/异常场景需要考虑？

## PRD 结构（PRD Structure）

生成的 PRD 应包含以下部分：

1.  简介/概览：简述功能及其解决的问题，并给出目标。
2.  目标（Goals）：列出具体、可衡量的目标。
3.  用户故事（User Stories）：描述用户如何使用功能及其收益。
4.  功能性需求（Functional Requirements）：逐条列出必须具备的功能，语言清晰简洁（例如：“系统必须允许用户上传头像”）。需编号。
5.  非目标（Out of Scope）：明确不在本次范围内的事项，以控制范围。
6.  设计考量（可选）：链接到设计稿、描述 UI/UX 要求，或注明相关组件/样式。
7.  技术考量（可选）：已知的技术约束、依赖或建议（例如：“应集成既有的 Auth 模块”）。
8.  成功度量（Success Metrics）：如何衡量该功能的成功（如“提升 10% 的用户互动”、“减少与 X 相关的工单”）。
9.  未决问题（Open Questions）：仍需澄清的问题或不确定点。

### 细节捕获提示（Detail Capture Hints）
为提升后续任务质量、降低细节遗漏，请尽可能补充以下内容（按需适用）：
- 边界与异常场景
- 外部集成（API、数据库、第三方服务）及期望契约
- 数据流程（校验、转换、持久化）
- 性能约束或目标
- 安全要求（认证/授权、输入校验/清理）
- UX 细节（加载/空状态/错误消息、可访问性）
- 分析/遥测需求（事件、指标、看板）
- 发布/运维考量（特性开关、迁移/回填、监控/告警）

## 目标读者（Target Audience）

假设 PRD 的主要读者是“初级开发者”。因此应做到：表述明确、不含歧义、尽量避免行话，同时提供足够细节帮助其理解功能目的与核心逻辑。

## 输出（Output）

- 格式：Markdown (`.md`)
- 位置：`/tasks/`
- 文件名：`prd-[feature-name].md`

## 最终指令（Final instructions）

1. 不要开始实现 PRD 所描述的功能。
2. 必须先向用户提问澄清问题。
3. 结合用户回答改进并完善 PRD。