# 规则：根据 PRD 生成任务清单

## 目标（Goal）

指导 AI 助手基于既有的产品需求文档（PRD），生成一份详细的、按步骤执行的任务清单（Markdown），用于指导开发者完成实现。

## 输出（Output）

- 格式：Markdown (`.md`)
- 位置：`/tasks/`
- 文件名：`tasks-[prd-file-name].md`（例如：`tasks-prd-user-profile-editing.md`）

## 流程（Process）

1.  接收 PRD 引用：用户指向具体的 PRD 文件。
2.  分析 PRD：阅读并分析所指 PRD 的功能需求、用户故事及其他相关章节。
3.  关键细节提取（Critical Details Extraction）：在继续之前，明确识别并记录：
    - 边界情况：有哪些错误条件、边界场景或异常情形被提及或隐含？
    - 集成点：需要集成哪些外部 API、数据库、服务或第三方系统？
    - 数据流：需要进行哪些数据转换、校验或持久化？
    - 性能要求：是否有明确的性能、可扩展性或优化要求？
    - 安全考量：认证、授权、数据校验等安全性要求？
    - 用户体验细节：加载状态、错误提示、反馈机制、可访问性要求？
5.  评估当前状态：审视现有代码库，了解基础设施、架构模式与约定，识别可复用或需修改的现有组件/功能；并罗列可利用或相关的文件、组件与工具。
6.  第一阶段：生成父任务：基于 PRD 分析与当前状态评估，创建任务文件并生成实现该功能所需的高层父任务。自行判断任务数量（通常约 5 个）。按指定格式呈现（暂不包含子任务）。并提示用户：“我已根据 PRD 生成高层任务。是否开始生成子任务？回复 ‘Go’ 继续。”
7.  等待确认：暂停，等待用户以“Go”确认。
8.  第二阶段：生成子任务：在用户确认后，将每个父任务拆解为可执行的小任务。注意：每个子任务必须显式覆盖第 3 步所识别的关键细节。子任务必须包含：
    - 对边界情况与错误场景的明确处理
    - 针对集成点的具体实现（含连接点/契约）
    - 数据的校验、转换与持久化步骤
    - 性能方面的考虑（缓存、优化等）
    - 安全实现（校验、清理、认证检查）
    - 用户体验元素（加载、错误处理、反馈）
7.  识别相关文件：基于任务与 PRD，识别可能需要创建或修改的文件，并列入“Relevant Files”部分（包括相应的测试文件）。
8.  生成最终输出：整合父任务、子任务、相关文件与说明，生成最终 Markdown 结构。
9.  保存任务清单：将文档保存至 `/tasks/` 目录，文件名为 `tasks-[prd-file-name].md`，其中 `[prd-file-name]` 与输入 PRD 的基础文件名一致（如输入为 `prd-user-profile-editing.md`，则输出为 `tasks-prd-user-profile-editing.md`）。

## 输出格式（Output Format）

生成的任务清单必须遵循如下结构：

```markdown
## 执行配置（Execution Configuration）

执行模式（Execution Mode）: `Default` | `Auto`
- Default：每个子任务后暂停等待用户确认
- Auto：在同一父任务内连续执行所有子任务，仅在父任务完成后暂停
- 质量门：无论模式如何，测试、质量检查与提交协议必须执行

## Relevant Files

AI 应按以下类别识别并列出文件：

- 核心实现文件：真正实现功能逻辑的主代码文件
- 测试文件：每个核心实现文件的对应测试
- 配置文件：构建、依赖或框架相关的配置文件
- 文档文件：API 文档、README 章节或其他需更新的文档
- 依赖文件：该功能所依赖或修改的共享工具库/模块

格式：`relative/path/to/file` - 对该文件在本功能中的角色进行简要说明

### Notes

AI 应提供面向项目的具体指引，遵循以下原则：

- 测试组织：根据项目既有模式说明测试应放置的位置
- 构建命令：识别在本项目中构建、测试与校验变更所需的命令
- 质量门禁：列出项目中配置的 lint、格式化、静态分析工具
- 依赖：注明需要安装的依赖或更新的包
- 集成点：突出与本功能集成的文件或系统

## Tasks

- [ ] 1.0 父任务标题
  - [ ] 1.1 [子任务描述 1.1]
        - 验收条件：列出可具体验证的结果
        - 失败案例：列出具有代表性的失败场景及期望处理方式
  - [ ] 1.2 [子任务描述 1.2]
        - 验收条件：列出可具体验证的结果
        - 失败案例：列出具有代表性的失败场景及期望处理方式
- [ ] 2.0 父任务标题
  - [ ] 2.1 [子任务描述 2.1]
- [ ] 3.0 父任务标题（若仅为结构/配置性任务，可不细分子任务）

## Detail Checklist

对每个父任务及其子任务，确保以下内容在适用范围内得到明确覆盖：
- [ ] 已识别并处理边界与错误流程
- [ ] 外部集成已明确连接点与契约
- [ ] 已包含数据校验/转换/持久化步骤
- [ ] 已考虑性能（缓存、批处理、分页等）
- [ ] 已覆盖安全（认证/授权/输入校验/清理）
- [ ] 已包含 UX 细节（加载、空状态、错误消息、可访问性）
```

## 交互模型（Interaction Model）

在生成父任务后必须暂停，等待用户以“Go”确认，再继续生成详细子任务。对于执行阶段，任务清单可在“执行配置（Execution Configuration）”中声明“执行模式（Execution Mode）”为 `Default` 或 `Auto`。当为 `Auto` 时，AI 将在同一父任务下连续执行所有子任务，并仅在父任务完成后暂停审核；质量门保持不变。在继续之前，AI 必须与用户一起审阅“关键细节提取”并确认一致性，确保高层计划与关键实现细节在执行前已达成共识。

## 目标读者（Target Audience）

假设任务清单的主要读者为熟悉现有代码库背景的“初级开发者”。

## 同步与状态指引（Sync & Status Guidance）
- 生成的任务清单需支持执行过程中的“即时状态更新”
- 每个子任务应可单独标记 `[ ]` → `[x]`，且若已为 `[x]` 则应可安全跳过
- 在“执行配置”中明确暂停/提交策略，便于执行方正确处理
- 鼓励设计为幂等子任务与原子提交，避免部分执行或重复工作