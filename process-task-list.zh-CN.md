# 任务清单管理（Task List Management）

用于基于 PRD 跟踪完成进度的 Markdown 任务清单管理指引

## 任务实施（Task Implementation）

### 执行模式（Execution Modes）

默认模式（逐步确认）：
- 单次仅处理一个子任务：在未征得用户允许前，请勿开始下一个子任务。必须先询问并得到用户回复“yes”或“y”。

自动模式（连续执行）：
- 当用户明确提出“自动模式/连续执行”（如：开启自动模式、Auto/Continuous），可在不逐个询问的情况下连续执行当前父任务下的所有子任务
- 仍需在父任务完成后暂停：当父任务的所有子任务完成后，必须暂停，等待用户审核再进入下一个父任务
- 质量门保持不变：仍需执行完整测试、质量检查与提交协议
- 完成协议：
  1. 当你完成一个子任务，立即将其标记为完成（将 `[ ]` 改为 `[x]`）。
  2. 当某个父任务下的所有子任务均为 `[x]` 时，按以下顺序执行：
    - 首先：使用项目中已配置的命令，运行项目的完整测试套件
    - 质量检查（Quality Check）：对照任务文件中的 Detail Checklist 验证实现是否到位：
      - [ ] 已处理边界情况与错误流程
      - [ ] 外部集成按正确连接点工作
      - [ ] 数据校验/转换/持久化正确运行
      - [ ] 已落实性能考量
      - [ ] 已覆盖安全问题
      - [ ] UX 细节按预期工作
    - 仅在“所有测试通过且清单验证无误”时：暂存变更（`git add .`）
    - 清理：在提交前移除任何临时代码与临时文件
    - 提交：使用清晰的提交信息，要求：
      - 遵循 Conventional Commits（如 `feat:`、`fix:`、`refactor:` 等）
      - 总结该父任务完成了什么
      - 列出关键改动与新增内容
      - 关联任务编号与 PRD 背景
      - 提交信息需以单行命令、通过多个 `-m` 参数组织，例如：

        ```
        git commit -m "feat: add payment validation logic" -m "- Validates card type and expiry" -m "- Adds unit tests for edge cases" -m "Related to T123 in PRD"
        ```
  3. 当所有子任务标记完成并提交后，将父任务标记为完成。
- 在每个子任务完成后暂停，等待用户继续指令。

## 任务清单维护（Task List Maintenance）

1. 按进度更新任务清单：
   - 依照上述协议标记任务/子任务为 `[x]`。
   - 发现新任务时及时补充。

2. 维护“Relevant Files”章节：
   - 列出每一个创建或修改的文件。
   - 为每个文件添加一句话说明其用途。

## AI 指令（AI Instructions）

当处理任务清单时，AI 必须：

1. 在完成任何重要工作后，及时更新任务清单文件。
2. 遵循完成协议：
   - 每完成一个子任务，将其标记为 `[x]`。
   - 当且仅当其所有子任务为 `[x]` 时，将父任务标记为 `[x]`。
3. 添加新发现的任务。
4. 保持“Relevant Files”信息准确、最新。
5. 在开始工作前，确认下一个待做的子任务。
6. 实现子任务后，更新文件并暂停，等待用户确认；若用户已开启“自动模式”，则同一父任务下的子任务之间无需暂停；但在父任务完成后仍需暂停供审核。
7. 在暂存或提交前，对照任务文件的“Detail Checklist”进行自检并解决遗漏。
8. 识别并遵循项目既有的测试、构建与质量工具（linters/formatters/static analysis）。