# AI Dev Tasks 用户手册（中文）

本手册面向使用 AI Dev Tasks 规则文件（PRD 生成、任务生成、任务执行）的人群，提供不同场景（已有项目/新建项目）的实操步骤、改进点与最佳实践。

## 适用对象
- 希望以结构化方式驱动 AI 辅助开发的个人或团队
- 以“初级开发者可执行”为目标的任务拆解与落地

## 快速开始

### 场景 A：集成到已有项目（Existing Project）
1) 放置规则文件：将以下文件放入仓库可访问位置（建议根目录或 /ai-dev-tasks）
   - create-prd.md / create-prd.zh-CN.md
   - generate-tasks.md / generate-tasks.zh-CN.md
   - process-task-list.md / process-task-list.zh-CN.md
2) 明确既有标准：识别项目已约定的命令与工具（测试/构建/质量门禁），确保后续严格复用：
   - 测试命令（如：npm test / pytest / mvn test 等）
   - 构建命令（如：npm run build / gradle build 等）
   - 质量门禁（lint/format/static analysis/type-check）
3) 基于 PRD 生成任务：使用 generate-tasks 进行“关键细节提取（Critical Details Extraction）”与“当前状态评估”：
   - 识别可复用组件与模块，避免重复造轮子
   - 列出相关文件清单（含测试文件）并遵循既有架构与约定
4) 执行任务：使用 process-task-list 按子任务粒度推进，严格执行“完成协议”和提交前的“Quality Check”。

建议改进点（已有项目）：
- 复用优先：在“评估当前状态”中强制列出可复用模块与依赖，减少重写
- 兼容性优先：对第三方依赖版本与项目基线进行兼容性检查（含类型/语言版本）
- 数据迁移与回滚：若涉及存储结构变更，提前生成迁移/回填/回滚任务
- 权限模型对齐：对齐既有认证/授权模型，避免新增路径绕过安全控制
- 性能预算：在 PRD/任务中量化性能目标（如 P95、吞吐、缓存策略）并将之落入验收标准

### 场景 B：新建项目（Greenfield/New Project）
1) 初始化工程基线：
   - 创建仓库与基础目录结构
   - 选择并配置包管理/依赖管理工具
   - 配置测试框架、lint、format、type-check、pre-commit 钩子
   - 定义标准脚本：test/build/lint/format/type-check 等
   - 可选：初始化 CI（测试、构建、质量门禁）
2) 先写 PRD：使用 create-prd（含“细节捕获提示”）完善需求细节
3) 生成任务：使用 generate-tasks 生成父任务与子任务，优先加入“项目脚手架”相关任务（如 0.x 序列）：
   - 0.1 初始化测试与质量工具
   - 0.2 定义项目脚本与基础目录
   - 0.3 配置 CI 与基础监控（可选）
4) 执行任务：使用 process-task-list 推进，每个子任务均需具备“验收条件”与“失败案例”。

建议改进点（新建项目）：
- 规范先行：在项目初期即确定编码规范、目录结构与质量门禁，写入任务清单
- 模板沉淀：形成 PRD 模板与任务模板（带 Detail Checklist 与验收/失败示例）
- 基础能力优先：优先实现日志、配置、错误处理、可观测性，再叠加业务功能
- 渐进交付：以小步快跑的子任务迭代，降低集成风险

## 操作指南（通用）

1) 编写 PRD（create-prd）
- 先提澄清问题，再产出 PRD
- 在“细节捕获提示”中补齐：边界/集成/数据流/性能/安全/UX/分析与运维

2) 生成任务（generate-tasks）
- 先执行“关键细节提取（Critical Details Extraction）”并与用户确认
- 阶段一：只生成父任务并暂停，等待“Go”
- 阶段二：生成子任务，必须包含：
  - 验收条件（可验证结果）
  - 失败案例（代表性失败场景及期望处理）
  - 显式覆盖 Detail Checklist 中六类细节

3) 执行任务（process-task-list）
- 单次仅处理一个子任务，完成后暂停等待确认
- 当父任务完成：运行全量测试 + 质量检查（Detail Checklist）
- 提交信息使用 Conventional Commits，逐项说明与任务/PRD 的关系

## 模板与清单

Detail Checklist（父任务与子任务均应覆盖适用项）：
- [ ] 边界与错误流程
- [ ] 外部集成连接点与契约
- [ ] 数据校验/转换/持久化
- [ ] 性能（缓存/批处理/分页等）
- [ ] 安全（认证/授权/输入校验/清理）
- [ ] UX（加载/空状态/错误消息/可访问性）

验收条件与失败案例示例：
- 表单保存：
  - 验收：合法数据持久化成功；错误消息按字段展示；保存按钮状态与加载反馈正确
  - 失败：后端 4xx/5xx；网络超时/重试；并发提交；非法输入（XSS/SQL 注入）
- 列表分页：
  - 验收：分页/排序/过滤正确；空态与错误态可见；性能达标（P95 < 200ms 示例）
  - 失败：越界页码；超大页大小；后端延迟/失败；缓存不一致
- 权限校验：
  - 验收：未授权访问被拒；不同角色权限矩阵正确；审计日志完整
  - 失败：令牌过期/伪造；权限遗漏路径；多角色边界

## 自动模式：连续执行（Auto Mode）

“自动模式”允许 AI 在同一父任务下连续执行所有子任务，无需逐个确认；仍会在父任务完成后暂停进行测试与质量检查，确保可控与高质量。

关键行为：
- 父任务内连续执行，无需子任务级暂停
- 父任务结束时暂停：运行全量测试 + Detail Checklist 自检
- 质量门禁保持不变：测试/清单/提交协议均照常执行

如何开启：
- 对话指令：使用“开启自动模式 / Auto Mode on / 启用连续执行”，关闭使用“关闭自动模式 / Auto Mode off”
- 任务文件：在文档头部“执行配置（Execution Configuration）”中声明

示例片段：
```
## 执行配置（Execution Configuration）
- 执行模式（Execution Mode）：Auto
- 暂停点（Pause Points）：仅在父任务完成时
- 质量门禁（Quality Gates）：提交前运行全量测试并完成 Detail Checklist
```

规则位置：
- process-task-list.md → 执行模式（Execution Modes）
- generate-tasks.md → 任务模板中的“执行配置”

适用场景：
- 父任务包含多个细小、低风险的子步骤
- 子任务的“验收条件/失败案例”定义清晰可靠
- 仍希望在父任务边界进行一次集中审核

注意事项：
- 不要跳过父任务级暂停；在继续前务必完成测试与清单自检
- 若涉及高风险外部依赖或数据迁移，优先使用默认模式（Default）
- 测试命令缺失：在任务中显式加入“探测并配置项目命令”的子任务
- 外部集成凭据：仅使用沙盒/本地环境变量，避免提交任何密钥到仓库
- 质量门禁失败：在任务中加入对应修复子任务（lint/format/type-check/单测）

## 最佳实践
- 小步提交：每个子任务形成最小可验证增量
- 先细节后拆解：先对齐 Critical Details，再拆子任务
- 人机协同：高层计划与关键细节均需用户确认
- 特性开关：重要改动以 Feature Flag 管控灰度

## 附录：规则文件
- 英文：create-prd.md / generate-tasks.md / process-task-list.md
- 中文：create-prd.zh-CN.md / generate-tasks.zh-CN.md / process-task-list.zh-CN.md